%table
  %thead
    %tr
      %th Category Name
  %tbody
    - @categories.each do |category|
      %tr
        %td= category.category_name
        %td= link_to 'Destroy', category_path(category), method: :delete,data: { confirm: 'Are you sure?' }